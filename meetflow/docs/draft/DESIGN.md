# Mリーグドラフト企画 設計メモ

> **このドキュメントの位置づけ**
>
> MeetFlow本体の設計書群（要件定義書・機能要件書・API設計書・DynamoDB物理設計書・Lambda設計書・画面設計書）とは**切り離して**管理する。
> 本体はMeetFlowのプロダクトコンセプト（相互非公開型マッチング／human-in-the-loop）に紐づく正式な仕様書であり、
> このドラフト企画は「麻雀コミュニティの遊び」として増築する趣味機能のため、本体の重い設計書運用には載せない。
>
> 本ドキュメントはブレスト（2026-09-17）の結論を次の実装セッションへ引き継ぐためのもの。**実装はまだ一切行っていない。**

---

## 1. 何を作るか

参加メンバー（3〜10人）でMリーグの選手をドラフト指名し、各自4人のチームを編成する。
Mリーグ2026-27レギュラーシーズンでの獲得ポイント合計で順位を競う。

Mリーグ本体のレギュレーションに倣い、**各自のチームに女性選手を必ず1人以上**組み込む。

機能は大きく2つ。

1. **ドラフト会議** — ターン制の指名、重複時の抽選、確定したチームの閲覧
2. **成績追跡** — Mリーグ公式等から結果を取得し、自分のチームの合計ポイントを表示

---

## 2. 前提となる事実

| 項目 | 値 | 確度 |
|---|---|---|
| シーズン | 2026-27 | 確定 |
| チーム数 | 10 | 確定 |
| 選手数 | 40（各チーム4人） | 確定 |
| 女性選手数 | **13** | 確定（`players_2026-27.json` で検証済み） |
| レギュラーシーズン開幕 | **2026-09-14**（2半荘消化済みの状態でブレスト実施） | ユーザー申告 |
| レギュラーシーズン終了 | **2027-03-26 前後** | 確度中（2025-26の実績が2026-03-27・第150節。2026-27の日程は当月分しか未公開） |
| ポストシーズン | 2027-04〜05 | セミファイナル約15節・ファイナル8節（2025-26実績） |

女性13人の内訳は、8チームが1人ずつ＋セガサミーフェニックス／KONAMI麻雀格闘倶楽部／BEAST Xが2人ずつ。

**この13という数字が女性枠ルール（§4.3）の成立条件に直結する。** 当初は10人と見積もっていたが実際は13人で、
参加者10人（理論上限）でも女流余剰枠が3残る。

---

## 3. 選手マスタ

`docs/draft/players_2026-27.json`（コミット済み）

- `teamId` / `teamName` / `name` / `isFemale` … 各チーム公式発表または報道で裏取り済み
- `kana` … 未検証（表示・ソート用のため影響は軽微）
- `organization` / `stats.*` … すべて `null`。スクレイパー実装時に埋める

### 性別フラグについて

**性別はどのサイトにも構造化データとして存在しない。** スクレイピングでは絶対に取得できないため、
40人中13人に手動でフラグを立てている。シーズンが変わったら手動でメンテナンスすること。

### 配置場所

現在は `docs/draft/` に置いているが、これは設計フェーズの暫定。
実装時は `MeetFlowDraftStack` のシードデータとして適切な場所（`backend/` 配下等）へ移す。

---

## 4. 決定事項

ブレストで確定した仕様。**実装者が独断で変更してはいけない。** 変更が必要ならユーザーに確認すること。

### 4.1 実施時期と集計範囲

- ドラフトは**開幕後**に実施する（シーズンは既に進行中）
- 集計は**シーズン全体**。ドラフト成立前に消化された分も遡って加算する
- 副作用として、ドラフト時点で各選手の今季成績が少し見えている状態になる（許容済み）

### 4.2 指名方式

**一斉指名＋抽選**（Mリーグ／NPB方式）。全4巡で4人のチームを編成する。

ラウンド `round`（1〜4）と、そのラウンド内の再指名回数 `wave`（1〜）の2軸で状態を持つ。
ユーザーの言う「1ターン」＝ 1つの `round` が確定するまで（wave が何回発生しても1ターン）。

```
SETUP
  ↓ 主催者が開始
NOMINATING(round=r, wave=w)   各自が1人を非公開で指名
  ↓ 全員提出 → 主催者が「開示」ボタン
REVEAL(r, w)                  一斉公開・重複を検出
  ├─ 重複なし → ラウンド確定
  └─ 重複あり → LOTTERY(r, w)
LOTTERY(r, w)                 重複選手ごとに抽選。当選者確定
  ↓ 落選者がいる
NOMINATING(round=r, wave=w+1) 落選者のみ再指名（確定済み選手は選択不可）
  ↓ ...繰り返し...
ラウンド確定 → round=r+1 へ（r=4 で COMPLETED）
```

### 4.3 女性枠ルール（動的強制）

基本は自由指名。ただし以下2つの制約がかかる。

**(a) 動的強制**

「残り巡数 ≦ 自分にまだ必要な女性数」になった参加者は、その巡から女性しか指名できない。
具体的には、3巡目終了時点で女性を1人も持っていない人は4巡目に女性強制となる。

**(b) 女流余剰枠**

```
余剰枠 B = 女性選手数(13) − 参加者数(N)
```

女性を既に確保している人が**追加で**女性を指名すると B が1減る。B=0 でブロック。

この設計で以下の不変条件が常に保たれるため、**詰みは原理的に発生しない**：

```
残り女流数 ≧ まだ女性を持っていない参加者数
```

> **証明の要点**：`残り女流数 = 未確保人数 + 残余剰枠` が初期状態で成立している。
> 必要指名は「残り女流数」と「未確保人数」を1ずつ減らし、追加指名は「残り女流数」と「残余剰枠」を1ずつ減らす。
> どちらの操作でも等式が保たれるため、残り女流数が未確保人数を下回ることはない。

したがって「女性は1人まで」という制限は**不要**。ブレスト途中でこの制限を提案したが撤回した。

参加者数ごとの余剰枠：

| 参加者 | 余剰枠 B |
|---|---|
| 3人 | 10 |
| 6人 | 7 |
| 8人 | 5 |
| 10人 | 3 |

### 4.4 余剰枠の超過は抽選で解決

一斉指名のため、同じ巡に複数人が追加指名して余剰枠を超過しうる（個別チェックは全員通るのに合計で超える）。
このときは**追加指名した人同士で余剰枠をかけて抽選**する。落選者は再指名。

結果として抽選は2種類になる：

1. **同一選手被りの抽選** — 複数人が同じ選手を指名
2. **女流余剰枠の抽選** — 別々の女流だが枠が足りない

どちらも同じくじ引き画面を流用する。

### 4.5 参加人数

3〜10人。上限は `min(10, 女性選手数)` で自動バリデーションする。
2026-27シーズンは女性13人なので、実質 `40 ÷ 4 = 10人` が上限。

### 4.6 進行主体

**主催者ボタン**で進める（自動遷移しない）。MeetFlow本体のhuman-in-the-loop原則と揃える。

- 全員提出後、主催者が「開示」を押して一斉公開
- 主催者が「抽選を実行」を押してくじ引き
- **主催者は参加者を兼任できる。** ただし開示前は主催者画面にも「4/4人 提出完了」しか出さない（中身は見せない）
- 主催者画面（プロジェクター／全体進行用）と参加者画面（スマホ／指名操作用）を分ける

### 4.7 タイムアウト

**制限時間は設けない。** 主催者画面に「未提出: Bさん」を表示して催促してもらう。
どうしても連絡がつかない場合のみ、主催者が**代理指名**できる。

### 4.8 成績取得

**完全オンデマンド。** EventBridge Scheduler も定期実行Lambdaも作らない。

- 誰かがアプリにアクセスして「更新」ボタンを押したときだけ取得する
- 未取得の日だけを取りにいく。最新取得日と同じなら何もしない
- 取得は**1日1回まで**。ただし失敗時はカウントを消費しない（リトライ可）

### 4.9 日付ロジック

```
最新確定日 = date(現在時刻 − 26時間)
```

ユーザー指定の「19時〜26時は前日扱い、前日を最新日としたい」を1本の式に畳んだもの。全パターンで一致する：

| 現在時刻 | 最新確定日 | 理由 |
|---|---|---|
| 7:00 | 前日 | 前夜の対局は終わっている |
| 20:00 | 前日 | 今夜の対局は進行中 |
| 深夜1:00 | 前々日 | 昨夜の対局がまだ進行中（25時扱い） |

**境界として実際に効くのは26時（翌2:00）だけ。** 19時は最新確定日の計算を変えない（18:59でも19:01でも答えは前日）。
19時は**「対局中」バッジの表示判定**に使う値として持つこと。

### 4.10 成績データ形式

**日別結果を積み上げる。** 対局日ごとの結果を日別レコードとして保存し、合計は自前で積算する。

日別レコードは3状態を区別すること：

- `対局あり` — 結果を取得済み
- `対局なし` — その日は対局がなかった（取得済み）
- `未取得` — まだ取りにいっていない

**`対局なし` を記録しないと、対局がなかった日を永久に取りにいき続ける。**

### 4.11 配置（完全分離）

```
MeetFlowDraftStack（新規・これ1つだけ追加）
  ├─ DraftTable          専用DynamoDBテーブル
  ├─ draft_lambda        8つ目のドメインLambda
  └─ DraftApi            専用API Gateway
                           └ Cognito Authorizer → 既存User Poolを「参照」するだけ

既存5スタック（data / auth / compute / api / frontend）
  → 1行も触らない

フロントエンド
  → 既存SPAに /draft ルートを追加（git revertで戻る）
  → APIのURLは環境変数を1本足すだけ
```

**この分離が重要な理由：**

1. **撤退可能性** — gitのブランチはコードしか戻さない。デプロイ済みインフラを消すには
   `cdk destroy MeetFlowDraftStack` が要る
2. **本体の設計の純度** — READMEで押している「単一テーブル設計への忠実さ／事前に洗い出した
   アクセスパターンの上に構築」を守る。ドラフトはアクセスパターンの性質が全く違う
   （短命・イベント的・数秒間隔で状態が変わる）

**撤退手順：**

1. `cdk destroy MeetFlowDraftStack` → テーブルごと消滅
2. フロントを `main` から再デプロイ
3. ブランチを消すか放置

### 4.12 リアルタイム更新

WebSocketは使わない。**ポーリング**（TanStack Query の `refetchInterval` 2〜3秒）で十分。

ドラフトは「10人 × 30分、年1回」のイベント。ドラフト状態に `version`（単調増加のリビジョン番号）を
持たせて、変わっていなければ軽量レスポンスを返す。

くじ引きの同期演出は、サーバーが抽選結果と一緒に `revealAt`（開封時刻）を返せば、
ポーリングでも全員の画面で同じ瞬間にドラムロールを止められる。

---

## 5. データモデル素案（未確定・たたき台）

専用テーブルなので本体のキー設計に縛られないが、単一テーブル的に持つなら：

```
Draft            PK=DRAFT#{draftId}  SK=METADATA
DraftParticipant PK=DRAFT#{draftId}  SK=PARTICIPANT#{userId}
DraftPick        PK=DRAFT#{draftId}  SK=PICK#{round}#{wave}#{userId}
DraftRoster      PK=DRAFT#{draftId}  SK=ROSTER#{userId}#{playerId}
PlayerLock       PK=DRAFT#{draftId}  SK=LOCK#{playerId}        ← 一意制約用
LotteryLog       PK=DRAFT#{draftId}  SK=LOTTERY#{round}#{wave}#{playerId}
MLPlayer         PK=MLPLAYER#{season} SK=PLAYER#{playerId}
MLPlayerDaily    PK=MLPLAYER#{season} SK=DAILY#{date}#{playerId}
```

**ミソは `PlayerLock`。** 「同じ選手を2人が確保する」事故を、条件付きPut（`attribute_not_exists`）で
物理的に不可能にするDynamoDBの定石パターン。

指名の受付は条件付き書き込みで「`phase=NOMINATING` かつ `round=r` かつ `wave=w` のときだけ」を担保する。
既存の `meetflow_common.transact` のパターンが流用できる。

---

## 6. 残論点（実装前に潰すこと）

### 6.1 ~~最優先：公式サイトの構造確認~~ → **調査完了（2026-09-18）**

**結果は `SCRAPING.md` にまとめた。結論だけ再掲する。**

- **§4.10（日別積み上げ）は成立する。** 累計スナップショット方式への後退は不要
- 対局日ごとの個別ページは存在せず、**`/games` 1枚に今季の全節の結果がモーダルとして埋め込まれている**。
  取得は1リクエストで完結し、日別の差分取得そのものが不要になる
- 日別結果を自前で積算した値が `/stats` の公式累計と**全24名で完全一致**（2026-09-18・3節消化時点で検証）
- 対局日カレンダーは `/games` の日程リストそのもの。`is-finish` の有無で「消化済み／未消化」、
  リストへの掲載有無で「対局あり／なし」が判定でき、**§4.10の3状態にそのまま対応する**
- robots.txt は今回使うページをすべて許可。規約上の制約は「商業利用の禁止」のみ（非商業の身内利用なら問題なし）
- 代替ソース（`mj-news.net` / `kinmaweb.jp`）は**不要**。公式だけで足りる

実装時に必ず踏襲すべき落とし穴が3つある。いずれも実測で踏んだもので、`SCRAPING.md` に詳しく書いた：

1. **文書内で最後のモーダルだけ閉じボタンの markup が無い** → 素直に書くと**最新の1節を取りこぼす**
2. **同じ日付に2節が入る日がある**（2025-26は131日中42日） → 日別レコードのキーは日付ではなく節にする
3. **`/stats` の選手名は `石井 一馬` と空白入り、`/games` 側は `石井一馬`** → 突き合わせ前に正規化が必須

### 6.2 その他

- [ ] **2種類の抽選の解決順** — 同一選手被りと女流余剰枠超過が同じ巡で同時発生したときの処理順
- [ ] **抽選の検証可能性** — 操作ログに残すだけか、commit-reveal（抽選前にseedのハッシュを公開し、
      結果と一緒にseedを開示）までやるか
- [x] ~~**対局日カレンダーの持ち方** — 「対局がない日」の判定方法~~ → `/games` の日程リストで確定（SCRAPING.md §2.1）
- [ ] **レギュラーシーズンとポストシーズンの切り分け** — §1は「レギュラーシーズンの合計」で競うと定めているが、日程HTMLにシリーズの区別が無い。`/stats` のRegularタブとの突き合わせで自動判定する案が有力（SCRAPING.md §4）。**要ユーザー判断**
- [ ] **`players_2026-27.json` の `teamId` が公式slugと3件ずれている** （`beastx`/`beast`、`mfc`/`fightclub`、`sakuranights`/`sakura**k**nights`）。公式に合わせるか `officialSlug` を別に持つか
- [ ] **`organization`（所属団体）はどこにも無い** — 手入力するか項目ごと落とすか
- [ ] **画面設計** — 主催者画面 / 参加者画面 / 成績画面
- [ ] **ポーリング間隔と version 番号の設計**
- [ ] **同時押しロック** — 2人が同時に成績更新ボタンを押したときの二重取得防止
      （DynamoDBの条件付き書き込みでロック）
- [x] ~~**初回押下者のレイテンシ**~~ → 常に `/games` 1リクエストで済むため解消（シーズン終盤でも1.6MB程度）
- [ ] **選手マスタのスナップショット** — ドラフトセッション作成時に選手リストを固定し、
      マスタが後から変わってもドラフト結果が壊れないようにする

---

## 7. スクレイピングの方針

### 選手マスタ

**ランタイムでスクレイピングしない。** 40人しかおらず、シーズン中はほぼ変動しない。

```
ローカルでスクレイプ → JSON生成 → リポジトリにコミット → デプロイ時にDynamoDBへシード
```

公式サイトへのアクセスは開発者が年1回叩くだけになり、規約リスクもHTML破損リスクもゼロ。

### 成績

§4.8 のとおり完全オンデマンド。1日1回上限。

**パース失敗は前提として設計すること：**

- 失敗時は古いデータを出し続けつつ、画面に「最終更新：MM/DD」を必ず出す
- 失敗時は既存の通知基盤で管理者にPush（本体の資産がそのまま効く）
- **手動入力のフォールバック画面を最初から作る。** これがあれば最悪シーズンは回る

### 使わないもの

- ロゴ・選手写真（著作権）。チームカラーで代替する

---

## 8. 次にやること

### ネットワーク設定 — ✅ 解決済み（2026-09-18 確認）

ブレスト時に `403 connect_rejected` で弾かれていた件は、ユーザーが claude.ai/code の環境設定で
**Custom + Allowed domains** を設定して解消した。以下がすべて疎通することを確認済み：

| ドメイン | 状態 |
|---|---|
| `m-league.jp` | ✅ 200（`/games` `/stats` `/teams/*` `/{YYYY}-season/` すべて取得可） |
| `mj-news.net` | ✅ 200（結果的に不要。公式だけで足りる） |
| `kinmaweb.jp` | ✅ 200（同上） |

弾かれたままのもの（現状の用途では不要）：
`ja.wikipedia.org` / `youtube.com` / `abema.tv` / `x.com` / `note.com` / 非公式スタッツサイト各種。

> 設定変更は既存セッションには遡って効かない。変更後は新しいセッションを立てること。
> 参考：https://code.claude.com/docs/en/cloud-environments#access-levels

### 作業順

1. ~~§6.1 の公式サイト構造調査~~ → **完了。`SCRAPING.md` 参照**
2. **§6.2 の残論点を潰す**（← いまここ）。特に以下はユーザー判断が要る：
   - レギュラー／ポストシーズンの切り分け方（SCRAPING.md §4）
   - 2種類の抽選の解決順
   - 抽選の検証可能性（操作ログのみ か commit-reveal まで やるか）
   - 画面設計（主催者／参加者／成績）
3. 実装用ブランチ `feature/mleague-draft` を切る
   （`claude/mleague-draft-brainstorm-oekkxm` はブレスト用、
   `claude/tender-sagan-pmrp9z` は本調査用）
4. `MeetFlowDraftStack` の実装に着手

### このリポジトリの `docs/draft/` の中身

| ファイル | 内容 |
|---|---|
| `DESIGN.md` | 本ドキュメント。決定事項と残論点 |
| `SCRAPING.md` | 公式サイトのデータ取得仕様（§6.1の調査結果） |
| `players_2026-27.json` | 選手マスタ。公式 `/stats` のロースターと**10チーム40名全員一致を検証済み**（2026-09-18） |
| `scrape_prototype.py` | パース規則の検証スクリプト。本番実装ではない。`python3 scrape_prototype.py` で公式サイトに対して検算が走る |

## 9. 参考リンク

- [Mリーグ2026-27シーズン Mリーガー全40名との選手契約合意のお知らせ](https://m-league.jp/news202607021500/)
- [Mリーグ2026-27シーズン、ドラフト会議開催および指名選手のお知らせ](https://m-league.jp/news202606251930/)
- [チーム・選手【Mリーグ 2026-27シーズン】- mj-news](https://mj-news.net/news/mleague/20260914259957)
- [成績・データ【Mリーグ 2026-27シーズン】- mj-news](https://mj-news.net/news/mleague/20260914259955)
